---
const { img, stars, product, ogprice, price} = Astro.props
---

<div class="bg-white h-[680px] aspect-[5/7] flex flex-col">
    <div class="w-full aspect-square overflow-hidden flex justify-start items-start">
        <img src={img} alt="Best Product" class="w-full object-cover" />
    </div>
    <div class="p-10 flex flex-col gap-4">
        <p class="text-2xl">{stars}</p>
        <h2 class="text-2xl">{product}</h2>
        <div class="flex gap-10 text-2xl">
            <p class="opacity-50 line-through">{ogprice}</p>
            <p>{price}</p>
        </div>
    </div>
</div>